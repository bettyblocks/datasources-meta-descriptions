import FormData from 'form-data'

const imanageUploadDocument = async ({documentName, documentId, authToken, file}) => {
    console.log({documentName, documentId, authToken, file})
}

export default imanageUploadDocument;